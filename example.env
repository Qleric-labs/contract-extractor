# API Keys
ANTHROPIC_API_KEY=sk-ant-api03-...

# -----------------------------------------------------------------------------
# Optional: Custom Extraction Prompts (The "Secret Sauce")
# -----------------------------------------------------------------------------
# If not set, the system uses default vanilla prompts.
#
# Available placeholders for CLAUDE_EXTRACTION_PROMPT:
# {tier_label} - e.g., "PROFESSIONAL"
# {fields_list} - The generated list of fields to extract
#
# Available placeholders for CLAUDE_RECHECK_PROMPT:
# No specific placeholders, but the code appends the missing fields list automatically.

CLAUDE_EXTRACTION_PROMPT="You are an expert contract analyst. Extract the requested fields into JSON format.\n\nEXTRACTION TIER: {tier_label}\n\nFIELDS TO EXTRACT:\n{fields_list}\n\n..."

CLAUDE_RECHECK_PROMPT="You are an expert contract analyst doing a SECOND PASS review..."
